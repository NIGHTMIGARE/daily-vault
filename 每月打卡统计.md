```dataview
table file.link as 日期, 
  choice(contains(text, "🎓") and regexmatch("- \\[x\\]", text), "✅", "❌") as 网课,
  choice(contains(text, "📖") and regexmatch("- \\[x\\]", text), "✅", "❌") as 电子书,
  choice(contains(text, "https://wio.xet.tech") and regexmatch("- \\[x\\]", text), "✅", "❌") as 题库WIO,
  choice(contains(text, "fangcaicoding") and regexmatch("- \\[x\\]", text), "✅", "❌") as 题库方才,
  choice(contains(text, "cheko.cc") and regexmatch("- \\[x\\]", text), "✅", "❌") as 题库车库,
  choice(contains(text, "🧱") and regexmatch("- \\[x\\]", text), "✅", "❌") as 知识整理,
  choice(contains(text, "🌐") and regexmatch("- \\[x\\]", text), "✅", "❌") as GitHub同步,
  choice(contains(text, "🏃") and regexmatch("- \\[x\\]", text), "✅", "❌") as 运动,
  choice(contains(text, "🇬🇧") and regexmatch("- \\[x\\]", text), "✅", "❌") as 英语
from "Daily"
where file.name != this.file.name
sort file.name desc
limit 30
```
